`volta` (2.0.2) was installed successfully!
## Notes
To use volta tab-completions, add following code to your `PowerShell PROFILE`:

```
(& volta completions powershell) | Out-String | Invoke-Expression
```
