## Lernziele
- Tupel erstellen und verwenden
- Unterschied zu Listen verstehen
- Tuple Unpacking beherrschen
- Richtige Anwendungsfälle erkennen
## Aufgabe 1: Tupel-Grundlagen

**Aufgabenstellung:**
Lernen Sie die Grundlagen von Tupeln durch praktische Übungen.

### Schritt 1: Tupel erstellen
```python
# 1. Erstellen Sie ein leeres Tupel
leeres_tupel = # Ihr Code hier

# 2. Erstellen Sie ein Tupel mit den Zahlen 1, 2, 3
zahlen_tupel = # Ihr Code hier

# 3. Erstellen Sie ein Ein-Element-Tupel mit der Zahl 42
ein_element = # Ihr Code hier

# 4. Erstellen Sie ein Tupel ohne Klammern: 10, 20, 30
ohne_klammern = # Ihr Code hier

print(f"Leer: {leeres_tupel}, Typ: {type(leeres_tupel)}")
print(f"Zahlen: {zahlen_tupel}, Typ: {type(zahlen_tupel)}")
print(f"Ein Element: {ein_element}, Typ: {type(ein_element)}")
print(f"Ohne Klammern: {ohne_klammern}, Typ: {type(ohne_klammern)}")
```

**💡 Lösungshinweise:**
- Leeres Tupel: `()`
- Mehrere Elemente: `(1, 2, 3)`
- Ein Element: `(42,)` - Komma ist wichtig!
- Ohne Klammern: `10, 20, 30`

### Schritt 2: Tupel vs Listen testen
```python
# Erstellen Sie eine Liste und ein Tupel mit gleichen Werten
liste = [1, 2, 3]
tupel = (1, 2, 3)

print(f"Liste: {liste}")
print(f"Tupel: {tupel}")

# Versuchen Sie die Liste zu ändern
liste[0] = 99
print(f"Liste nach Änderung: {liste}")

# Versuchen Sie das Tupel zu ändern
try:
    tupel[0] = 99  # Das sollte einen Fehler geben
except # Welcher Fehler-Typ?:
    print("Fehler: Tupel können nicht geändert werden!")
```

**💡 Lösungshinweis:** Der Fehler-Typ ist `TypeError`

### Schritt 3: Tupel-Methoden
```python
# Gegeben ist dieses Tupel:
buchstaben = ('a', 'b', 'c', 'b', 'd', 'b', 'e')

# Zählen Sie, wie oft 'b' vorkommt
anzahl_b = buchstaben.count(# Ihr Code)

# Finden Sie die Position des ersten 'b'
position_b = buchstaben.index(# Ihr Code)

print(f"Buchstaben: {buchstaben}")
print(f"Anzahl 'b': {anzahl_b}")
print(f"Position erstes 'b': {position_b}")
```

**💡 Lösungshinweise:**
- `tupel.count("element")` zählt Vorkommen
- `tupel.index("element")` findet die Position
## Aufgabe 2: Tuple Unpacking

**Aufgabenstellung:**
Üben Sie das Entpacken von Tupeln in verschiedenen Situationen.

### Schritt 1: Einfaches Unpacking
```python
# Gegeben sind diese Tupel:
punkt_2d = (15, 25)
person = ("Anna", 22, "Studentin", "Berlin")

# Entpacken Sie den 2D-Punkt
x, y = # Ihr Code
print(f"Punkt: x={x}, y={y}")

# Entpacken Sie die Person-Informationen
name, alter, beruf, stadt = # Ihr Code
print(f"Person: {name}, {alter} Jahre, {beruf} aus {stadt}")
```

### Schritt 2: Variablen tauschen
```python
# Gegeben sind diese Variablen:
a = 100
b = 200
c = 300

print(f"Vorher: a={a}, b={b}, c={c}")

# Tauschen Sie a und b (ohne Hilfsvariable!)
a, b = # Ihr Code

# Rotieren Sie alle drei Variablen: a→b, b→c, c→a
a, b, c = # Ihr Code

print(f"Nach Tausch: a={a}, b={b}, c={c}")
```

**💡 Lösungshinweise:**
- Tauschen: `a, b = b, a`
- Rotation: `a, b, c = c, a, b`

### Schritt 3: Funktionen mit Tupel-Rückgabe
```python
def get_name_age():
    """Gibt Name und Alter als Tupel zurück"""
    return "Bob", 25  # Tupel ohne Klammern

def berechne_rechteck(laenge, breite):
    """Berechnet Umfang und Fläche eines Rechtecks"""
    umfang = 2 * (laenge + breite)
    flaeche = laenge * breite
    return # Ihr Code (Tupel mit umfang und flaeche)

# Entpacken Sie die Rückgabewerte
name, alter = # Ihr Code
print(f"Name: {name}, Alter: {alter}")

u, f = # Ihr Code für berechne_rechteck(5, 3)
print(f"Rechteck 5x3: Umfang={u}, Fläche={f}")
```
## Aufgabe 3: Erweiterte Unpacking-Patterns

**Aufgabenstellung:**
Lernen Sie erweiterte Unpacking-Techniken mit dem `*` Operator.

### Ihre Aufgaben:
```python
# Gegeben sind diese Tupel:
zahlen = (1, 2, 3, 4, 5, 6, 7, 8)
alphabet = ('a', 'b', 'c', 'd', 'e', 'f')

# 1. Erstes Element, alles andere in 'rest'
erste, *rest = # Ihr Code
print(f"Erste: {erste}, Rest: {rest}")

# 2. Erstes, letztes Element, alles andere in 'mitte'
erste, *mitte, letzte = # Ihr Code
print(f"Erste: {erste}, Mitte: {mitte}, Letzte: {letzte}")

# 3. Erste zwei Elemente, dann Rest
erste, zweite, *rest = # Ihr Code
print(f"Erste zwei: {erste}, {zweite}, Rest: {rest}")

# 4. Alle außer den letzten zwei
*anfang, vorletzte, letzte = # Ihr Code
print(f"Anfang: {anfang}, Letzte zwei: {vorletzte}, {letzte}")

# 5. Nur erstes und letztes (Rest ignorieren)
erste, *_, letzte = # Ihr Code
print(f"Nur erste und letzte: {erste}, {letzte}")
```

**💡 Lösungshinweise:**
- `*rest` sammelt den Rest in eine Liste
- `*_` ignoriert den Rest (underscore = "unwichtig")
- Der `*` kann an verschiedenen Positionen stehen
## Aufgabe 4: Koordinaten-System

**Aufgabenstellung:**
Arbeiten Sie mit 2D- und 3D-Koordinaten als Tupel.

### Schritt 1: 2D-Punkte
```python
def erstelle_punkt(x, y):
    """Erstellt einen 2D-Punkt als Tupel"""
    return # Ihr Code

def punkte_addieren(punkt1, punkt2):
    """Addiert zwei 2D-Punkte"""
    x1, y1 = # Entpacken Sie punkt1
    x2, y2 = # Entpacken Sie punkt2
    return # Neuer Punkt als Tupel

def punkt_info(punkt):
    """Zeigt Informationen über einen Punkt"""
    x, y = # Entpacken Sie den Punkt
    print(f"Punkt: ({x}, {y})")

# Testen Sie Ihre Funktionen
punkt_a = erstelle_punkt(2, 3)
punkt_b = erstelle_punkt(5, 7)

punkt_info(punkt_a)
punkt_info(punkt_b)

summe = punkte_addieren(punkt_a, punkt_b)
punkt_info(summe)
```

### Schritt 2: 3D-Punkte
```python
def erstelle_3d_punkt(x, y, z):
    """Erstellt einen 3D-Punkt als Tupel"""
    return # Ihr Code

def entfernung_3d(punkt1, punkt2):
    """Berechnet die Entfernung zwischen zwei 3D-Punkten"""
    x1, y1, z1 = # Entpacken Sie punkt1
    x2, y2, z2 = # Entpacken Sie punkt2
    
    dx = x2 - x1
    dy = y2 - y1
    dz = z2 - z1
    
    return (dx*dx + dy*dy + dz*dz) ** 0.5

# Test
punkt1 = erstelle_3d_punkt(0, 0, 0)
punkt2 = erstelle_3d_punkt(3, 4, 0)
distanz = entfernung_3d(punkt1, punkt2)
print(f"Entfernung: {distanz}")
```

**💡 Lösungshinweise:**
- Tupel erstellen: `(x, y)` oder `(x, y, z)`
- Unpacking: `x, y = punkt`
- Distanz-Formel: √((x₂-x₁)² + (y₂-y₁)²)
## Aufgabe 5: Datenbank-Simulation

**Aufgabenstellung:**
Simulieren Sie eine einfache Datenbank mit Tupeln als Datensätze.

### Schritt 1: Datenbank erstellen
```python
# Erstellen Sie eine "Studenten-Datenbank" als Liste von Tupeln
# Jeder Student: (ID, Vorname, Nachname, Studiengang, Semester)
studenten_db = [
    (1, "Anna", "Müller", "Informatik", 3),
    (2, "Bob", "Schmidt", "Mathematik", 2),
    (3, "Charlie", "Wagner", "Physik", 4),
    (4, "Diana", "Fischer", "Informatik", 1)
]

# Zeigen Sie alle Studenten formatiert an
print("Studenten-Datenbank:")
print("ID | Vorname  | Nachname | Studiengang  | Semester")
print("-" * 50)

for # Entpacken Sie jeden Studenten:
    print(f"{student_id:2} | {vorname:8} | {nachname:8} | {studiengang:12} | {semester}")
```

### Schritt 2: Datenbankoperationen
```python
def finde_student_nach_id(datenbank, gesuchte_id):
    """Findet einen Studenten anhand der ID"""
    for student in datenbank:
        student_id, vorname, nachname, studiengang, semester = # Entpacken
        if # Prüfen Sie die ID:
            return student
    return None

def studenten_nach_studiengang(datenbank, studiengang):
    """Findet alle Studenten eines Studiengangs"""
    gefunden = []
    for student in datenbank:
        # Entpacken Sie den Studenten
        _, vorname, nachname, student_studiengang, semester = # Ihr Code
        if # Prüfen Sie den Studiengang:
            gefunden.append(student)
    return gefunden

# Testen Sie Ihre Funktionen
student = finde_student_nach_id(studenten_db, 2)
if student:
    _, vorname, nachname, studiengang, semester = student
    print(f"Student mit ID 2: {vorname} {nachname}")

informatiker = studenten_nach_studiengang(studenten_db, "Informatik")
print(f"Informatik-Studenten: {len(informatiker)}")
for student in informatiker:
    _, vorname, nachname, _, semester = student
    print(f"  {vorname} {nachname} (Semester {semester})")
```

### Schritt 3: Statistiken
```python
def berechne_statistiken(datenbank):
    """Berechnet Statistiken über die Studenten"""
    if not datenbank:
        return "Keine Daten"
    
    semester_liste = []
    studiengaenge = []
    
    for student in datenbank:
        # Entpacken Sie den Studenten
        _, _, _, studiengang, semester = # Ihr Code
        semester_liste.append(semester)
        if studiengang not in studiengaenge:
            studiengaenge.append(studiengang)
    
    return {
        "anzahl_studenten": len(datenbank),
        "durchschnitt_semester": sum(semester_liste) / len(semester_liste),
        "min_semester": min(semester_liste),
        "max_semester": max(semester_liste),
        "anzahl_studiengaenge": len(studiengaenge),
        "studiengaenge": studiengaenge
    }

stats = berechne_statistiken(studenten_db)
print("\nStatistiken:")
for key, value in stats.items():
    if isinstance(value, float):
        print(f"  {key}: {value:.1f}")
    else:
        print(f"  {key}: {value}")
```

**💡 Lösungshinweise:**
- Entpackung in Schleife: `for id, name, ... in liste:`
- Vergleiche: `if student_id == gesuchte_id:`
- Listen für Sammlung: `gefunden.append(student)`
## Erfolgskontrolle

Nach diesen Aufgaben sollten Sie können:
- [ ] Verschiedene Arten von Tupeln erstellen (leer, ein Element, mehrere)
- [ ] Den Unterschied zwischen Listen und Tupeln erklären
- [ ] Tupel-Methoden `count()` und `index()` anwenden
- [ ] Einfaches Tuple Unpacking durchführen
- [ ] Variablen elegant mit Tupeln tauschen
- [ ] Erweiterte Unpacking-Patterns mit `*` verwenden
- [ ] Funktionen schreiben, die Tupel zurückgeben
- [ ] Mit Tupeln als Datensätze arbeiten
- [ ] Entscheiden, wann Tupel statt Listen sinnvoll sind
