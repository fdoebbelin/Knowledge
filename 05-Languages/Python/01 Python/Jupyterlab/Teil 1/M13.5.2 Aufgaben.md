## Lernziele
- Eigene Funktionen für Text-Verarbeitung entwickeln
- Strukturierte Daten aus Texten extrahieren
- Wiederverwendbare String-Tools erstellen
- Performance-bewusste Text-Verarbeitung implementieren
## Aufgaben

### Aufgabe 1: Smart Text Cleaner

Erstellen Sie eine Sammlung von Text-Bereinigungs-Funktionen.

**Was Sie tun sollen:**
```python
# Zelle 1: Smart Text Cleaner
def bereinige_email(email):
    """Bereinigt und validiert E-Mail-Adressen"""
    # TODO: 
    # 1. Leerzeichen entfernen
    # 2. Zu Kleinbuchstaben
    # 3. Prüfen ob gültige E-Mail (mit RegEx)
    # 4. Rückgabe: (bereinigte_email, ist_gueltig)
    pass

def bereinige_telefonnummer(telefon):
    """Bereinigt deutsche Telefonnummern"""
    # TODO:
    # 1. Alle Nicht-Ziffern entfernen (außer + am Anfang)
    # 2. Deutsche Vorwahl normalisieren (0 → +49)
    # 3. Leerzeichen zur besseren Lesbarkeit einfügen
    # Beispiel: "0 30 / 123-456" → "+49 30 123456"
    pass

def bereinige_text_komplett(text, optionen=None):
    """Vollständige Text-Bereinigung mit Optionen"""
    # TODO: Bereinige Text nach diesen Optionen:
    # - "leerzeichen": Mehrfache Leerzeichen zu einem
    # - "gross_klein": "klein", "gross", oder "title"  
    # - "sonderzeichen": Entfernt alle außer Buchstaben/Zahlen
    # - "zahlen": Entfernt alle Zahlen
    # Standard-Optionen: {"leerzeichen": True, "gross_klein": "klein"}
    pass

# Test-Daten
test_emails = ["  MAX@TEST.DE  ", "ungueltig@", "anna@beispiel.com"]
test_telefone = ["0 30 / 123-456", "+49171/9876543", "030-12345678"]
test_text = "   HALLO!!!   Das  ist  ein   TEXT123  mit   vielen    PROBLEMEN!!!   "
```

**💡 Lösungshinweise:**
- **E-Mail RegEx:** `r'^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'`
- **Telefon bereinigen:** `re.sub(r'[^\d+]', '', telefon)` für Nicht-Zahlen
- **Deutsche Vorwahl:** `telefon.replace('0', '+49', 1)` nur am Anfang
- **Default-Parameter:** `if optionen is None: optionen = {...}`
- **Mehrfache Leerzeichen:** `re.sub(r'\s+', ' ', text)`
### Aufgabe 2: Configuration Parser

Entwickeln Sie einen Parser für verschiedene Konfigurations-Formate.

**Was Sie tun sollen:**
```python
# Zelle 2: Configuration Parser
# Zelle 2: Configuration Parser
def parse_key_value_config(config_text):
    """Parst einfache Key=Value Konfigurationen"""
    # TODO:
    # 1. Zeilen aufteilen und leere/Kommentar-Zeilen überspringen
    # 2. Key=Value Paare finden und trennen
    # 3. Dictionary mit allen Konfigurationswerten zurückgeben (alle als String)
    pass

def parse_section_config(config_text):
    """Parst Konfiguration mit Sektionen [section]"""
    # TODO:
    # 1. Sektionen [section_name] erkennen
    # 2. Key=Value Paare der jeweiligen Sektion zuordnen
    # 3. Listen-Format "key: item1, item2" unterstützen
    # 4. Verschachteltes Dictionary zurückgeben (alle Werte als String/Liste)
    pass

def extrahiere_csv_daten(csv_text, delimiter=','):
    """Extrahiert strukturierte Daten aus CSV-Text"""
    # TODO:
    # 1. Header-Zeile von Daten-Zeilen trennen
    # 2. Jede Zeile in Dictionary umwandeln (Header als Keys)
    # 3. Liste von Dictionaries zurückgeben (alle Werte als String)
    pass

# Test-Daten
config_data = """
# Beispiel-Konfiguration
database_host = localhost
database_port = 5432
debug_mode = true
max_connections = 100

[server]
host = 0.0.0.0
port = 8080
ssl_enabled = false
users: anna, bob, charlie
"""

csv_data = """Name,Alter,Stadt,E-Mail
Anna Schmidt,25,Berlin,anna@test.de
Bob Mueller,30,München,bob@example.com
Charlie Wagner,28,Hamburg,charlie@domain.org"""
```

**💡 Lösungshinweise:**
- **Zeilen-Iteration:** `for zeile in config_text.split('\n'):`
- **Kommentare überspringen:** `if zeile.startswith('#'): continue`
- **Sektionen erkennen:** `if zeile.startswith('[') and zeile.endswith(']'):`
- **CSV-Header:** `header = zeilen[0].split(delimiter)`
### Aufgabe 3: Text Analytics Engine

Erstellen Sie ein System zur Text-Analyse mit verschiedenen Metriken.

**Was Sie tun sollen:**
```python
# Zelle 3: Text Analytics Engine
def analysiere_text_statistiken(text):
    """Umfassende Text-Statistiken"""
    # TODO:
    # 1. Zeichen zählen (gesamt und ohne Leerzeichen)
    # 2. Wörter, Sätze, Absätze zählen
    # 3. Wortlängen-Statistiken (Durchschnitt, längstes, kürzestes)
    # 4. Readability-Score berechnen
    # 5. Dictionary mit allen Statistiken zurückgeben
    pass

def finde_haeufige_woerter(text, top_n=5, min_laenge=3):
    """Findet die häufigsten Wörter im Text"""
    # TODO:
    # 1. Text bereinigen (Satzzeichen entfernen)
    # 2. Stoppwörter und kurze Wörter filtern
    # 3. Wort-Häufigkeiten zählen
    # 4. Top N Wörter sortiert zurückgeben
    pass

def extrahiere_entities(text):
    """Extrahiert Entitäten (E-Mails, URLs, Telefonnummern)"""
    # TODO:
    # 1. E-Mail-Adressen mit RegEx finden
    # 2. URLs (http/https) extrahieren
    # 3. Deutsche Telefonnummern erkennen
    # 4. Zahlen und großgeschriebene Wörter finden
    # 5. Dictionary mit allen gefundenen Entitäten
    pass

# Test-Text
beispiel_text = """
Python ist eine großartige Programmiersprache! Sie ist leicht zu lernen und sehr vielseitig.
Viele Unternehmen wie Google und Instagram nutzen Python für ihre Anwendungen.

Wenn Sie Fragen haben, kontaktieren Sie uns unter info@python-kurs.de oder besuchen Sie 
https://www.python.org. Sie können uns auch unter +49 30 12345678 anrufen.

Der Kurs kostet 299.99 Euro und dauert 40 Stunden. Das ist ein sehr guter Preis!
"""
```

**💡 Lösungshinweise:**
- **Sätze zählen:** `re.split(r'[.!?]+', text)`
- **Absätze zählen:** `text.split('\n\n')`
- **Stoppwörter:** `{'der', 'die', 'das', 'und', 'oder', 'aber'}`
- **URL-Pattern:** `r'https?://[A-Za-z0-9.-]+\.[A-Za-z]{2,}[/\w]*'`
- **Telefon-Pattern:** `r'(\+49[\s-]?|0)(\d{2,5})[\s-]?(\d{6,8})'`
### Aufgabe 4: Text Processing Pipeline

Entwickeln Sie ein System für verkettbare Text-Verarbeitung.

**Was Sie tun sollen:**
```python
# Zelle 4: Text Processing Pipeline
class TextProcessor:
    """Klasse für verkettbare Text-Verarbeitung"""
    
    def __init__(self, text):
        self.text = text
        self.history = [text]  # Protokolliert alle Änderungen
    
    def bereinigen(self, entferne_sonderzeichen=True, normalisiere_leerzeichen=True):
        """Bereinigt den Text"""
        # TODO:
        # 1. Leerzeichen normalisieren wenn gewünscht
        # 2. Sonderzeichen entfernen wenn gewünscht
        # 3. Text aktualisieren und zu History hinzufügen
        # 4. self zurückgeben für Verkettung
        pass
    
    def normalisieren(self, case='lower'):
        """Normalisiert Groß-/Kleinschreibung"""
        # TODO: case kann 'lower', 'upper', oder 'title' sein
        pass
    
    def filtern(self, min_wortlaenge=1, entferne_stoppwoerter=False):
        """Filtert Wörter nach Kriterien"""
        # TODO:
        # 1. Text in Wörter aufteilen
        # 2. Nach Wortlänge filtern
        # 3. Stoppwörter entfernen wenn gewünscht
        # 4. Wörter wieder zusammenfügen
        pass
    
    def ergebnis(self):
        """Gibt das finale Ergebnis zurück"""
        return self.text
    
    def zeige_schritte(self):
        """Zeigt alle Verarbeitungsschritte"""
        # TODO: History formatiert ausgeben
        pass

def verarbeite_batch_texte(texte, verarbeitungs_funktion):
    """Verarbeitet mehrere Texte mit derselben Funktion"""
    # TODO: List Comprehension für Batch-Verarbeitung
    pass

def text_pipeline(*verarbeitungsschritte):
    """Erstellt eine wiederverwendbare Pipeline"""
    # TODO:
    # 1. Pipeline-Funktion definieren
    # 2. Alle Schritte nacheinander auf Text anwenden
    # 3. Pipeline-Funktion zurückgeben
    pass

# Test-Texte
beispiel_texte = [
    "  HALLO Welt!!! Das ist ein TEST.  ",
    "Python ist GROSSARTIG und sehr MÄCHTIG!",
    "  Ich lerne gerne neue PROGRAMMIERSPRACHEN  !"
]
```

**💡 Lösungshinweise:**
- **Verkettung:** `return self` ermöglicht `obj.methode1().methode2()`
- **History:** `self.history.append(self.text)` nach jeder Änderung
- **Stoppwörter-Set:** `{'der', 'die', 'das', 'und', 'oder', 'aber', 'ist', 'sind'}`
- **Pipeline-Pattern:** Funktionen nacheinander auf Ergebnis anwenden
- **Batch-Processing:** `[func(text) for text in texte]`
## ✅ Erfolgs-Checkliste

- [ ] **Aufgabe 1:** E-Mails validiert, Telefonnummern normalisiert, Text flexibel bereinigt
- [ ] **Aufgabe 2:** Key-Value und Section-Configs geparst, CSV-Daten extrahiert
- [ ] **Aufgabe 3:** Text-Statistiken berechnet, häufige Wörter gefunden, Entitäten extrahiert
- [ ] **Aufgabe 4:** Verkettbare Pipeline implementiert, Batch-Verarbeitung funktioniert
- [ ] **Code-Qualität:** Funktionen haben Docstrings und sind gut strukturiert
- [ ] **Tests:** Alle Funktionen mit Test-Daten getestet und validiert

## 🧪 Test-Strategie

**Testen Sie jede Aufgabe schrittweise:**

1. **Implementieren** Sie eine Funktion
2. **Testen** Sie mit den bereitgestellten Test-Daten
3. **Debuggen** Sie bei Problemen mit `print()`-Statements
4. **Erweitern** Sie um zusätzliche Test-Fälle
5. **Optimieren** Sie für bessere Performance oder Lesbarkeit
