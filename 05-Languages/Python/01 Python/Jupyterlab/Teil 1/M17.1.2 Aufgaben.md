## 🎯 Lernziele
- PEP 8 Namenskonventionen korrekt anwenden
- Code-Formatierung nach PEP 8 Standards durchführen
- Häufige Stil-Fehler erkennen und korrigieren
- Unordentlichen Code nach PEP 8 überarbeiten
- PEP 8 Checkliste praktisch verwenden

## 📋 Szenario
Du überarbeitest verschiedene Code-Fragmente eines Taschenrechner-Projekts, um sie PEP 8-konform zu machen. Dabei lernst du, wie sauberer Code-Stil die Lesbarkeit und Wartbarkeit verbessert.

```python
# Zelle 0: Testdaten vorbereiten
def bereite_test_vor():
    """Bereitet das Test-Szenario vor"""
    print("📐 PEP 8 Code-Stil Verbesserung")
    print("=" * 35)
    print("Aufgaben:")
    print("1. Namenskonventionen korrigieren")
    print("2. Code-Formatierung verbessern")
    print("3. Schlechten Code überarbeiten")
    print("4. PEP 8 Checkliste anwenden")
    print("\nViel Erfolg beim Code-Styling! ✨")

# Test der Vorbereitung
bereite_test_vor()
```

## 🔧 Aufgabe 1: Namenskonventionen korrigieren

### Schritt 1.1: Variablennamen verbessern
```python
# Zelle 1.1: Schlechte Variablennamen nach PEP 8 korrigieren
def korrigiere_variablennamen():
    """Korrigiert Variablennamen nach PEP 8 snake_case Standard"""
    
    # ❌ Schlechte Namen (nicht PEP 8 konform)
    StudentName = "Anna Schmidt"
    studentAge = 22
    IsActive = True
    maxPoints = 100
    
    # TODO: Benenne alle Variablen nach snake_case um
    # TODO: student_name = StudentName
    # TODO: student_age = studentAge  
    # TODO: is_active = IsActive
    # TODO: max_points = maxPoints
    # TODO: Gib ein Dictionary mit korrigierten Namen zurück
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(korrigiere_variablennamen())
```

### Schritt 1.2: Funktionsnamen verbessern
```python
# Zelle 1.2: Funktionsnamen nach PEP 8 korrigieren
def korrigiere_funktionsnamen():
    """Korrigiert Funktionsnamen nach PEP 8 Standards"""
    
    # ❌ Schlechte Funktionsnamen
    def CalcAverage(numbers):
        return sum(numbers) / len(numbers)
    
    def checkGrade(grade):
        return grade < 4.0
    
    def PrintResult(result):
        print(f"Ergebnis: {result}")
    
    # TODO: Erstelle neue Funktionen mit korrekten snake_case Namen:
    # TODO: berechne_durchschnitt(), pruefe_note(), zeige_ergebnis()
    # TODO: Teste die neuen Funktionen mit Beispieldaten
    # TODO: Gib die Ergebnisse zurück
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(korrigiere_funktionsnamen())
```

### Schritt 1.3: Konstanten und Klassen benennen
```python
# Zelle 1.3: Konstanten und Klassennamen korrigieren
def korrigiere_konstanten_und_klassen():
    """Korrigiert Konstanten- und Klassennamen nach PEP 8"""
    
    # ❌ Schlechte Konstanten und Klassen
    pi = 3.14159
    maxStudents = 30
    
    class calculator:
        def __init__(self):
            self.result = 0
    
    class studentManager:
        def __init__(self):
            self.students = []
    
    # TODO: Korrigiere Konstanten zu UPPER_CASE
    # TODO: PI = pi, MAX_STUDENTS = maxStudents
    # TODO: Korrigiere Klassen zu PascalCase
    # TODO: Calculator, StudentManager
    # TODO: Erstelle Instanzen und teste sie
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(korrigiere_konstanten_und_klassen())
```

## 🔧 Aufgabe 2: Code-Formatierung verbessern

### Schritt 2.1: Leerzeichen korrigieren
```python
# Zelle 2.1: Leerzeichen nach PEP 8 korrigieren
def korrigiere_leerzeichen():
    """Korrigiert Leerzeichen-Verwendung nach PEP 8"""
    
    # ❌ Schlechte Leerzeichen-Verwendung
    x=10+20*30
    liste=[1,2,3,4,5]
    dict_data={'name':'Anna','age':22}
    
    # TODO: Korrigiere die Leerzeichen:
    # TODO: x = 10 + 20 * 30
    # TODO: liste = [1, 2, 3, 4, 5]
    # TODO: dict_data = {'name': 'Anna', 'age': 22}
    # TODO: Teste eine Funktion: max(liste) statt max (liste)
    # TODO: Gib alle korrigierten Werte zurück
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(korrigiere_leerzeichen())
```

### Schritt 2.2: Einrückung standardisieren
```python
# Zelle 2.2: Einrückung auf 4 Leerzeichen korrigieren
def korrigiere_einrueckung():
    """Korrigiert Einrückung nach PEP 8 (4 Leerzeichen)"""
    
    # TODO: Schreibe eine Funktion mit korrekter 4-Leerzeichen-Einrückung
    # TODO: def analysiere_zahlen(zahlen):
    # TODO:     if not zahlen:  # 4 Leerzeichen
    # TODO:         return "Keine Zahlen"  # 8 Leerzeichen
    # TODO:     
    # TODO:     summe = sum(zahlen)  # 4 Leerzeichen
    # TODO:     if summe > 50:  # 4 Leerzeichen
    # TODO:         return "Hoch"  # 8 Leerzeichen
    # TODO:     else:  # 4 Leerzeichen
    # TODO:         return "Niedrig"  # 8 Leerzeichen
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(korrigiere_einrueckung())
```

### Schritt 2.3: Lange Zeilen aufteilen
```python
# Zelle 2.3: Zeilenlänge auf maximal 79 Zeichen begrenzen
def korrigiere_zeilenlaenge():
    """Teilt lange Zeilen nach PEP 8 auf (max. 79 Zeichen)"""
    
    # ❌ Zu lange Zeile
    langer_text = "Das ist ein sehr langer Text der definitiv über 79 Zeichen lang ist und aufgeteilt werden sollte"
    
    # TODO: Teile den Text auf mehrere Zeilen auf:
    # TODO: langer_text = (
    # TODO:     "Das ist ein sehr langer Text der definitiv über 79 Zeichen "
    # TODO:     "lang ist und aufgeteilt werden sollte"
    # TODO: )
    
    # TODO: Teile auch diese Funktionsdefinition auf:
    # TODO: def erstelle_student_info(name, alter, studiengang, durchschnittsnote):
    # TODO: wird zu:
    # TODO: def erstelle_student_info(
    # TODO:     name, alter, studiengang, durchschnittsnote
    # TODO: ):
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(korrigiere_zeilenlaenge())
```

## 🔧 Aufgabe 3: Schlechten Code überarbeiten

### Schritt 3.1: Unordentlichen Taschenrechner-Code verbessern
```python
# Zelle 3.1: Kompletten schlechten Code nach PEP 8 überarbeiten
def ueberarbeite_taschenrechner():
    """Überarbeitet schlechten Taschenrechner-Code nach PEP 8"""
    
    # ❌ Sehr schlechter Code (alle PEP 8 Regeln verletzt)
    def calc(a,b,op):
        if op=="+":\
    return a+b
        elif op=="-":
     return a-b
        elif op=="*":return a*b
        else:return a/b
    
    # TODO: Schreibe den Code komplett neu nach PEP 8:
    # TODO: - Korrekte Funktionsnamen (snake_case)
    # TODO: - Richtige Einrückung (4 Leerzeichen)
    # TODO: - Leerzeichen um Operatoren
    # TODO: - Lesbare Struktur
    # TODO: - Teste die neue Funktion
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(ueberarbeite_taschenrechner())
```

### Schritt 3.2: Schlechte Klasse überarbeiten
```python
# Zelle 3.2: Schlecht formatierte Klasse nach PEP 8 korrigieren
def ueberarbeite_student_klasse():
    """Überarbeitet eine schlecht formatierte Klasse"""
    
    # ❌ Schlechte Klasse
    class student:
        def __init__(self,n,a):self.n=n;self.a=a
        def getInfo(self):return f"{self.n},{self.a}"
        def isAdult(self):return self.a>=18
    
    # TODO: Schreibe die Klasse neu nach PEP 8:
    # TODO: - Klassenname: PascalCase (Student)
    # TODO: - Methodennamen: snake_case (get_info, is_adult)
    # TODO: - Korrekte Einrückung und Leerzeichen
    # TODO: - Aussagekräftige Parameter- und Attributnamen
    # TODO: - Teste die neue Klasse
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(ueberarbeite_student_klasse())
```

## 🔧 Aufgabe 4: PEP 8 Checkliste anwenden

### Schritt 4.1: Code-Review durchführen
```python
# Zelle 4.1: Gemischten Code nach PEP 8 Checkliste überprüfen
def fuehre_code_review_durch():
    """Führt ein Code-Review mit PEP 8 Checkliste durch"""
    
    # Zu überprüfender Code
    MaxValue=100
    def CheckValue(val):
        if val>MaxValue:return False
        else:return True
    
    class dataProcessor:
        def processData(self,data):
            result=[]
            for item in data:
                if CheckValue(item):result.append(item*2)
            return result
    
    # TODO: Erstelle eine PEP 8 Checkliste für den Code:
    # TODO: - Namenskonventionen prüfen
    # TODO: - Leerzeichen prüfen
    # TODO: - Einrückung prüfen
    # TODO: - Zeilenlänge prüfen
    # TODO: - Gib eine Liste der gefundenen Probleme zurück
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(fuehre_code_review_durch())
```

### Schritt 4.2: Perfekten PEP 8 Code erstellen
```python
# Zelle 4.2: Neuen Code vollständig PEP 8-konform schreiben
def erstelle_perfekten_code():
    """Erstellt von Grund auf PEP 8-konformen Code"""
    
    # TODO: Erstelle ein kleines Noten-Verwaltungssystem:
    # TODO: - Konstante: MAX_NOTE = 6.0
    # TODO: - Funktion: berechne_durchschnitt(noten_liste)
    # TODO: - Funktion: bewerte_note(note)
    # TODO: - Klasse: NotenManager
    # TODO: - Alles perfekt nach PEP 8 formatiert
    # TODO: - Teste das komplette System
    pass
    
    return "Funktion noch nicht implementiert"

# Test der Funktion
print(erstelle_perfekten_code())
```

## 💡 Lösungshinweise

### Namenskonventionen:
```python
# Variablen und Funktionen: snake_case
student_name = "Anna"
def berechne_durchschnitt():
    pass

# Konstanten: UPPER_CASE
MAX_STUDENTS = 30
PI = 3.14159

# Klassen: PascalCase
class StudentManager:
    pass
```

### Leerzeichen-Regeln:
```python
# ✅ Richtig
x = a + b
liste = [1, 2, 3]
func(param)

# ❌ Falsch
x=a+b
liste=[1,2,3]
func (param)
```

### Einrückung:
```python
# 4 Leerzeichen pro Ebene
def funktion():
    if bedingung:
        return ergebnis
    else:
        return alternative
```

## ✅ Erwartete Ausgaben

**Schritt 1.1 - Variablennamen:**
```
Korrigierte Namen: {'student_name': 'Anna Schmidt', 'student_age': 22, 'is_active': True, 'max_points': 100}
```

**Schritt 2.1 - Leerzeichen:**
```
Korrigierte Werte: x=600, liste=[1, 2, 3], max=5
```

**Schritt 3.1 - Taschenrechner:**
```
Taschenrechner Test: 10 + 5 = 15, 10 - 5 = 5, 10 * 5 = 50, 10 / 5 = 2.0
```

**Schritt 4.1 - Code-Review:**
```
PEP 8 Probleme gefunden: ['MaxValue sollte MAX_VALUE sein', 'CheckValue sollte check_value sein', 'dataProcessor sollte DataProcessor sein']
```

## 🎯 Erfolgskriterien
- [ ] Alle Namen folgen PEP 8 Konventionen
- [ ] Leerzeichen sind korrekt um Operatoren platziert
- [ ] Einrückung verwendet genau 4 Leerzeichen
- [ ] Keine Zeilen sind länger als 79 Zeichen
- [ ] Code ist deutlich lesbarer als vorher
- [ ] PEP 8 Checkliste kann praktisch angewendet werden