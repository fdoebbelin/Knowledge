## 🎯 Lernziele
- Variable Argumentlisten für flexible APIs verstehen
- *args für beliebige Anzahl von Positionsargumenten nutzen
- **kwargs für variable Schlüsselwortargumente einsetzen
- Kombination beider für maximale Flexibilität beherrschen
- Argument-Entpackung mit * und ** anwenden

---

## 📋 Aufgabenstellung

### Problemstellung
Sie entwickeln ein Notenverwaltungssystem für eine Schule, das flexibel verschiedene Bewertungsszenarien unterstützen soll. Das System muss beliebig viele Noten verarbeiten können und gleichzeitig optionale Gewichtungen für verschiedene Fächer oder Kategorien berücksichtigen.

### Beispieldaten
```python
# Beispieldaten für Tests
einzelnoten_deutsch = [2.3, 1.7, 2.0, 1.3, 2.7]
einzelnoten_mathe = [1.0, 1.3, 2.0, 1.7]
einzelnoten_englisch = [2.0, 2.3, 1.7, 2.0, 1.3, 2.3]

fach_gewichtungen = {
    "deutsch": 0.3,
    "mathematik": 0.4, 
    "englisch": 0.2,
    "sport": 0.1
}

kategorie_gewichtungen = {
    "klausuren": 0.6,
    "muendlich": 0.2,
    "hausaufgaben": 0.2
}
```

### Erwartete Ausgabe
```
=== NOTENDURCHSCHNITT BERECHNUNG ===
Einzelnoten: 2.3, 1.7, 2.0, 1.3, 2.7
Anzahl Noten: 5
Durchschnitt: 2.00
Gewichtungen angewendet: deutsch (30%), mathematik (40%)
Gewichteter Durchschnitt: 1.85
```

---

## 🔧 Implementierungsschritte

### Schritt 1: Grundfunktion mit *args

**Aufgabe:** Implementieren Sie eine Funktion `berechne_durchschnitt()` die beliebig viele Noten als Positionsargumente akzeptiert:

```python
# Zelle 1: Grundfunktion mit *args implementieren
def berechne_durchschnitt(*noten):
    """
    Berechnet den Durchschnitt aus beliebig vielen Noten.
    
    Parameter:
    - *noten: Beliebige Anzahl von Noten (float/int)
    
    Rückgabe:
    - float: Durchschnittsnote oder None bei leerer Eingabe
    """
    # TODO: Implementierung hier
    pass

# Test der Grundfunktion
print("🧪 Test 1: Einzelne Noten als Parameter")
durchschnitt1 = berechne_durchschnitt(2.3, 1.7, 2.0, 1.3, 2.7)
print(f"Durchschnitt: {durchschnitt1}")

print("\n🧪 Test 2: Unterschiedliche Anzahl Noten")
durchschnitt2 = berechne_durchschnitt(1.0, 1.3, 2.0)
print(f"Durchschnitt: {durchschnitt2}")

print("\n🧪 Test 3: Leere Eingabe")
durchschnitt3 = berechne_durchschnitt()
print(f"Durchschnitt: {durchschnitt3}")
```

**💡 Lösungshinweis:** 
- Verwenden Sie `*noten` um beliebig viele Argumente zu akzeptieren
- `noten` ist dann ein Tupel mit allen übergebenen Werten
- Behandeln Sie den Fall leerer Eingabe (len(noten) == 0)
- Nutzen Sie `sum()` und `len()` für die Berechnung

---

### Schritt 2: Erweiterte Funktion mit **kwargs

**Aufgabe:** Erweitern Sie die Funktion um optionale Gewichtungen über **kwargs:

```python
# Zelle 2: Erweiterte Funktion mit **kwargs
def berechne_gewichteten_durchschnitt(*noten, **gewichtungen):
    """
    Berechnet Durchschnitt mit optionalen Gewichtungen.
    
    Parameter:
    - *noten: Beliebige Anzahl von Noten
    - **gewichtungen: Optionale Gewichtungen als key=value
    
    Beispiel:
    berechne_gewichteten_durchschnitt(2.0, 1.5, 2.5, 
                                     klausur=0.6, muendlich=0.4)
    """
    # TODO: Implementierung hier
    # - Grunddurchschnitt berechnen
    # - Prüfen ob Gewichtungen vorhanden
    # - Gewichtete Berechnung durchführen
    # - Informative Ausgabe erstellen
    pass

# Test der erweiterten Funktion
print("🧪 Test 1: Ohne Gewichtungen")
berechne_gewichteten_durchschnitt(2.3, 1.7, 2.0, 1.3, 2.7)

print("\n🧪 Test 2: Mit Gewichtungen")
berechne_gewichteten_durchschnitt(2.0, 1.5, 2.5, 
                                 klausur=0.6, muendlich=0.4)

print("\n🧪 Test 3: Viele Gewichtungen")
berechne_gewichteten_durchschnitt(1.7, 2.0, 2.3, 1.3,
                                 deutsch=0.3, mathe=0.4, 
                                 englisch=0.2, sport=0.1)
```

**💡 Lösungshinweis:**
- **kwargs ist ein Dictionary mit allen Keyword-Argumenten
- Prüfen Sie ob Gewichtungen vorhanden sind: `if gewichtungen:`
- Summieren Sie die Gewichtungswerte: `sum(gewichtungen.values())`
- Normalisieren Sie die Gewichtungen falls nötig (Summe != 1.0)

---

### Schritt 3: Fach-spezifische Notenverwaltung

**Aufgabe:** Erstellen Sie ein System für fach-spezifische Notenverwaltung:

```python
# Zelle 3: Fach-spezifische Notenverwaltung
def verwalte_fachnoten(**faecher_mit_noten):
    """
    Verwaltet Noten für verschiedene Fächer.
    
    Parameter:
    - **faecher_mit_noten: Fach=Notenliste Paare
    
    Beispiel:
    verwalte_fachnoten(deutsch=[2.3, 1.7, 2.0], 
                      mathe=[1.0, 1.3, 2.0])
    """
    # TODO: Implementierung
    # - Für jedes Fach Durchschnitt berechnen
    # - Übersichtliche Ausgabe erstellen
    # - Gesamtdurchschnitt über alle Fächer
    pass

def erstelle_zeugnis(*fach_durchschnitte, **zusatz_infos):
    """
    Erstellt ein Zeugnis mit flexiblen Parametern.
    
    Parameter:
    - *fach_durchschnitte: Durchschnittsnoten der Fächer
    - **zusatz_infos: Zusätzliche Informationen (Name, Klasse, etc.)
    """
    # TODO: Implementierung
    # - Zeugnisformat erstellen
    # - Fach-Durchschnitte anzeigen
    # - Zusatzinfos einbinden
    # - Gesamtnote berechnen
    pass

# Test der fach-spezifischen Verwaltung
print("🧪 Test 1: Mehrere Fächer mit Noten")
verwalte_fachnoten(
    deutsch=[2.3, 1.7, 2.0, 1.3, 2.7],
    mathematik=[1.0, 1.3, 2.0, 1.7],
    englisch=[2.0, 2.3, 1.7, 2.0, 1.3]
)

print("\n🧪 Test 2: Zeugnis erstellen")
erstelle_zeugnis(2.0, 1.5, 1.8, 2.2,  # Fach-Durchschnitte
                name="Max Mustermann", 
                klasse="10a", 
                schuljahr="2024/25",
                bemerkung="Gute Leistungen")
```

**💡 Lösungshinweis:**
- Iterieren Sie über `faecher_mit_noten.items()` für Fach-Noten-Paare
- Nutzen Sie die bereits implementierte `berechne_durchschnitt` Funktion
- Erstellen Sie eine schöne tabellarische Ausgabe
- Verwenden Sie f-Strings für formatierte Ausgabe

---

### Schritt 4: Argument-Entpackung und flexible APIs

**Aufgabe:** Implementieren Sie eine Funktion die sowohl Listen als auch einzelne Werte verarbeiten kann:

```python
# Zelle 4: Flexible Notenverwaltung mit Entpackung
def flexibler_notenrechner(modus="einfach", *args, **kwargs):
    """
    Flexibler Rechner der verschiedene Eingabeformen verarbeitet.
    
    Modi:
    - "einfach": Einfacher Durchschnitt
    - "gewichtet": Gewichteter Durchschnitt 
    - "faecher": Fach-spezifische Berechnung
    - "zeugnis": Vollständiges Zeugnis
    """
    # TODO: Implementierung basierend auf Modus
    pass

def entpacke_und_berechne(notenlisten, gewichtungs_dict=None):
    """
    Hilfsfunktion für Argument-Entpackung.
    
    Parameter:
    - notenlisten: Liste oder Tupel mit Notenlisten
    - gewichtungs_dict: Dictionary mit Gewichtungen
    """
    # TODO: Verwendet * und ** für Entpackung
    pass

# Test mit Listen-Entpackung
print("🧪 Test 1: Listen entpacken")
noten_liste = [2.3, 1.7, 2.0, 1.3, 2.7]
# Entpackung mit *
durchschnitt = berechne_durchschnitt(*noten_liste)
print(f"Entpackter Durchschnitt: {durchschnitt}")

print("\n🧪 Test 2: Dictionary entpacken")
gewichtungen = {"klausur": 0.6, "muendlich": 0.3, "hausaufgaben": 0.1}
# Entpackung mit **
berechne_gewichteten_durchschnitt(2.0, 1.5, 2.5, **gewichtungen)

print("\n🧪 Test 3: Flexible Modi")
flexibler_notenrechner("einfach", 2.3, 1.7, 2.0)
flexibler_notenrechner("gewichtet", 2.0, 1.5, 2.5, 
                      klausur=0.7, muendlich=0.3)
flexibler_notenrechner("faecher", 
                      deutsch=[2.3, 1.7], 
                      mathe=[1.0, 1.3])
```

**💡 Lösungshinweis:**
- Verwenden Sie Pattern Matching oder if-elif für verschiedene Modi
- `*args` fängt alle Positionsargumente nach `modus` ab
- `**kwargs` sammelt alle Keyword-Argumente
- Für Entpackung: `func(*liste)` und `func(**dict)`

---

### Schritt 5: Fortgeschrittene Statistik-Funktionen

**Aufgabe:** Erweitern Sie das System um statistische Analysefunktionen:

```python
# Zelle 5: Erweiterte Statistik-Funktionen
def erweiterte_notenstatistik(*noten_gruppen, **analyse_optionen):
    """
    Führt erweiterte statistische Analyse durch.
    
    Parameter:
    - *noten_gruppen: Mehrere Notengruppen zum Vergleich
    - **analyse_optionen: Optionen für die Analyse
      * zeige_details: Detaillierte Ausgabe
      * runde_auf: Rundung der Ergebnisse
      * include_median: Median berechnen
      * include_std: Standardabweichung berechnen
    """
    # TODO: Implementierung
    # - Statistiken für jede Gruppe berechnen
    # - Vergleiche zwischen Gruppen
    # - Optionale Berechnungen basierend auf analyse_optionen
    pass

def note_zu_punkte(*noten, punkte_system="standard"):
    """
    Konvertiert Noten in Punktesystem.
    
    Punkte-Systeme:
    - "standard": 1=15, 2=12, 3=9, 4=6, 5=3, 6=0
    - "abi": Abitur-Punktesystem
    - "uni": Universitätssystem
    """
    # TODO: Implementierung der Konvertierung
    pass

def kombiniere_bewertungen(*bewertungs_tupel, normalisierung=True):
    """
    Kombiniert verschiedene Bewertungsarten.
    
    Parameter:
    - *bewertungs_tupel: (note, gewichtung) Tupel
    - normalisierung: Gewichtungen automatisch normalisieren
    """
    # TODO: Implementierung
    pass

# Test der erweiterten Funktionen
print("🧪 Test 1: Erweiterte Statistik")
gruppe_a = [2.3, 1.7, 2.0, 1.3, 2.7]
gruppe_b = [1.0, 1.3, 2.0, 1.7, 1.5]
gruppe_c = [2.0, 2.3, 1.7, 2.0, 1.3]

erweiterte_notenstatistik(gruppe_a, gruppe_b, gruppe_c,
                         zeige_details=True,
                         runde_auf=2,
                         include_median=True)

print("\n🧪 Test 2: Noten-Punkte Konvertierung")
note_zu_punkte(2.3, 1.7, 2.0, punkte_system="standard")
note_zu_punkte(1.0, 1.3, 2.0, punkte_system="abi")

print("\n🧪 Test 3: Bewertungen kombinieren")
kombiniere_bewertungen(
    (2.0, 0.4),  # Klausur, 40%
    (1.5, 0.3),  # Mündlich, 30%
    (2.5, 0.3),  # Hausaufgaben, 30%
    normalisierung=True
)
```

**💡 Lösungshinweis:**
- Implementieren Sie Helper-Funktionen für Median, Standardabweichung
- Verwenden Sie Dictionaries für Punkte-System-Mappings
- Nutzen Sie List Comprehensions für Konvertierungen
- Validieren Sie Gewichtungen bei der Kombination

---

## ✅ Erfolgskriterien

Ihre Lösung ist erfolgreich, wenn:

1. **Variable Positionsargumente:** *args wird korrekt für beliebige Notenanzahl verwendet
2. **Variable Keyword-Argumente:** **kwargs ermöglicht flexible Gewichtungen
3. **Kombination beider:** Funktionen nutzen sowohl *args als auch **kwargs sinnvoll
4. **Argument-Entpackung:** Listen und Dictionaries werden mit * und ** entpackt
5. **Flexible APIs:** Verschiedene Eingabeformen werden elegant verarbeitet
6. **Fehlerbehandlung:** Leere Eingaben und ungültige Gewichtungen werden behandelt

---

## 🎯 Bonus-Herausforderungen

Wenn Sie die Grundaufgabe gemeistert haben:

1. **Validierung:** Fügen Sie umfassende Eingabevalidierung hinzu
2. **Export-Funktionen:** Implementieren Sie CSV/JSON Export für Noten
3. **Batch-Verarbeitung:** Verarbeiten Sie mehrere Schüler gleichzeitig
4. **Visualisierung:** Erstellen Sie einfache ASCII-Charts für Notenverteilung

---

## 📚 Wichtige Konzepte in dieser Übung

- ***args (Positionsargumente):** Sammelt beliebige Anzahl Argumente in Tupel
- ****kwargs (Keyword-Argumente):** Sammelt benannte Argumente in Dictionary
- **Argument-Entpackung:** `*liste` und `**dict` für Parameterübergabe
- **Flexible API-Design:** Funktionen die verschiedene Eingabeformen akzeptieren
- **Parameter-Reihenfolge:** Richtige Anordnung von args, kwargs und anderen Parametern