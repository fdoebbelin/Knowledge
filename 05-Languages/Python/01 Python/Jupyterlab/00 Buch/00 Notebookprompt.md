## neu für Claude
```
finde alle Python-Codebereiche auch auch die Interpreter-Prompts der bereitgestellen PDF-Datei, mache daraus ein Markdown-Dokument mit eingebetteten Python-Codezellen (die Marker ```python und ``` immer ganz links ausrichten), die Erklärung in Stichpunkten über den Codezellen und alle Pythoncodebeispiele immer in Funktionen kapseln und mit unterschiedlichen Argumenten aufrufen, das ganze so aufteilen das jedes Code-Beispiel in einer eigenen passend benannten Funktion angezeigt wird und das Ergebnis über return zurückgeben, ohne print arbeiten, da die Markdown-Datei in ein Jupyter notebook konvertiert werden soll, ein dem die Funktionsaufrufe für sich allein in einer mit einem Kommentar und den Argumenten Zuweisungen versehenen Codezelle für sich allein stehen sollen, der Interpreter zeigt so immer den berechneten Wert an
```


```
bitte die Funktionsaufrufe den Deklarationen zuordnen und bitte das ganze so aufteilen das jedes Code-Beispiel in einer eigenen passend benannten Funktion angezeigt wird und das Ergebnis über return zurückgeben
```



```
bitte eine Markdown Datei mit allen Codebeispielen
```
